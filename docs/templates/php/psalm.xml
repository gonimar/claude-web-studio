<?xml version="1.0"?>
<!-- Psalm — Web Studio template (stack-reference/php.md). `php_static_analysis: psalm` (the yiisoft ecosystem's
     own choice). errorLevel 1 is the strictest; a brownfield project starts from `psalm.xml` generated by
     `vendor/bin/psalm --init src 3` and lowers the level one step per story. -->
<psalm errorLevel="1" resolveFromConfigFile="true" findUnusedCode="true" findUnusedBaselineEntry="true"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="https://getpsalm.org/schema/config"
       xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd">
    <projectFiles>
        <directory name="src"/>
        <directory name="tests"/>
        <ignoreFiles>
            <directory name="vendor"/>
        </ignoreFiles>
    </projectFiles>
</psalm>
